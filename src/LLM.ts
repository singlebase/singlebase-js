
/**
 * LLM
 * - generate()
 * - embeddings(str|str[])
 * - search(query:str, filters:{})
 * - store()
 * - retrieve()
 */

export default class LLM {
  constructor() {
    throw new Error("LLM NOT YET IMPLEMENTED")
  }
}
